class AppConstants {
  static const String appName = 'Vibia';
  static const double defaultPadding = 20.0;
  static const double defaultRadius = 15.0;
  static const Duration defaultAnimationDuration = Duration(milliseconds: 300);
}
